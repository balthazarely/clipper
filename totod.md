- need to add how we can limit or give the server rules as to what i can and cant do
- need to figure out if we can create common workflows
- need to fiure out how we can use the command prompt menu with all the detials

inside MPC_setup, for a usecase can we add creating types from an API that doesnt have defined types. also pls add something about being careful with client data or anything sentistve. Also add somethig about how we can make chanegs to the CSS in the browser, and bring those edits back into the codwebase
